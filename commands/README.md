# Commands

The canonical command contract lives in:
- `commands/atomic_command_registry.json`

Legacy compatibility mirror:
- `00_SISTEMA/_meta/atlas/atomic_command_registry.json`

This directory holds the root-first command contract and future command specs.

Current read-only commands exposed through the minimal dispatcher:
- `audit-repo`
- `certify-project`
- `surface-audit`
