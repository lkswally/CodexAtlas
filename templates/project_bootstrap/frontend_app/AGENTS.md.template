# AGENTS for {{project_name}}

## Project Context
- This is an Atlas-derived `{{project_type}}` scaffold.
- Runtime-specific frontend decisions must stay local to this derived project.

## Working Boundaries
- Keep Atlas governance and mother configuration outside this repo.
- Define audience, visual direction and component boundaries before implementation.
- Do not enable deploy automation, MCPs or hidden hooks during bootstrap.

## Recommended Roles
- `ux_brand` for audience and direction.
- `architect` for module and component boundaries.
- `reviewer` for usability and regression risk.

## First Workflow
- Document the target audience and UX direction in README.md and docs/.
- Use app/, ui/ and assets/ only as structure placeholders until the design direction is explicit.
- Keep implementation decisions subordinate to the documented product intent.
