# AGENTS for {{project_name}}

## Project Context
- This is an Atlas-derived `{{project_type}}` scaffold.
- Runtime-specific operational logic must stay local to this derived project.

## Working Boundaries
- Keep Atlas governance and mother configuration outside this repo.
- Prefer workflow clarity, reporting and documentation before implementation.
- Do not enable deploy automation, MCPs or hidden hooks during bootstrap.

## Recommended Roles
- `planner` for workflow definition.
- `implementer` for safe utility slices.
- `reality_checker` for assumptions and evidence.

## First Workflow
- Capture the operator workflow in README.md and docs/.
- Use automation/, reports/ and ops/ as structure placeholders until the workflow is explicit.
- Keep any later implementation changes aligned with documented ownership.
