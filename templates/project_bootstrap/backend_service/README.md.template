# {{project_name}}

Atlas-derived Backend Service scaffold.

## Purpose
{{project_goal}}

## Project Profile
- Profile: `{{project_type}}`
- Bootstrap source: `{{generated_from_skill}}`
- Atlas root: `{{atlas_root}}`

## Initial Scope
{{scope}}

## Suggested Structure
- `docs/`
- `memory/`
- `workflows/`
- `policies/`
- `tools/`
- `api/`
- `services/`
- `tests/`

## Example Usage
1. Document the initial API contract in `docs/` before adding endpoints.
2. Use `workflows/` to define release and verification steps without automating deploy.

## Safety Boundaries
- Keep Atlas core outside this derived project.
- Do not add MCP, deploy automation or dependency installers during bootstrap.
- Use this scaffold to clarify interfaces and ownership before implementation starts.

## Next Steps
- Define the service purpose and owner.
- Clarify API and operational boundaries.
- Keep integration surfaces explicit before implementation.
- Build a validation-first roadmap in `docs/`.
