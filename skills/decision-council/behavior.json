{
  "writes_files": false,
  "writes_code": false,
  "uses_output_dir": false,
  "read_only": true,
  "execution_helper": "decision_council_report",
  "side_effects": [
    "returns a structured decision report without modifying files",
    "can include local phase or intent evidence when a project path is provided"
  ],
  "requires_project_path": false,
  "requires_output_dir": false,
  "can_run_without_approval": true,
  "notes": [
    "the skill helps structure difficult decisions; it does not execute the outcome",
    "approval is still required when the resulting decision would widen scope or permissions"
  ]
}
